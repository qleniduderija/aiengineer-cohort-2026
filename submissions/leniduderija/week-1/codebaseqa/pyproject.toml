[project]
name = "codebaseqa"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
authors = [
    { name = "Leni Đuderija", email = "leni.duderija@q.agency" }
]
requires-python = ">=3.13"
dependencies = [
    "anthropic>=1.0.0",
    "python-dotenv>=1.2.3",
]

[project.scripts]
codebaseqa = "codebaseqa:main"

[build-system]
requires = ["uv_build>=0.12.5,<0.13.0"]
build-backend = "uv_build"
