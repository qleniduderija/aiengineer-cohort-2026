# Anthropic / course gateway (same as weeks 1-2)
CLAUDE_MODEL=
ANTHROPIC_API_KEY=
API_ENDPOINT_BASE_URL=

# BookStack (kb.q.agency) API — user profile > API Tokens
KB_BASE_URL=
KB_TOKEN_ID=
KB_TOKEN_SECRET=

# Local Postgres+pgvector (matches docker-compose.yml)
DATABASE_URL=postgresql://kbagent:kbagent@localhost:5432/kbagent

# Embeddings — run locally via sentence-transformers (see README: gateway has no embedding model in its allowlist)
EMBEDDING_MODEL=BAAI/bge-small-en-v1.5
